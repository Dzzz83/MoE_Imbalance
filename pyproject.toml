[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "gpu: requires a CUDA device",
  "slow: long-running tests that are excluded from quick checks",
  "integration: exercises multiple project components together",
]

